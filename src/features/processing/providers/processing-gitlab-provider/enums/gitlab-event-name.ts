export enum GitlabEventName {
  PUSH_HOOK = 'Push Hook',
  TAG_PUSH_HOOK = 'Tag Push Hook',
  ISSUE_HOOK = 'Issue Hook',
  COMMENT_HOOK = 'Note Hook',
  MERGE_REQUEST_HOOK = 'Merge Request Hook',
  PIPELINE_HOOK = 'Pipeline Hook',
  JOB_HOOK = 'Job Hook',
}
